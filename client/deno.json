{
  "tasks": {
    "dev": "deno run -A npm:vite",
    "dev:electron": "npx electron electron/dev.js",
    "dev:window": "deno task dev & npx electron electron/dev.js",
    "build": "deno run -A npm:vite build",
    "build:electron": "deno run -A electron/build_electron.ts",
    "install": "npm i vite @sveltejs/vite-plugin-svelte fdir sass node canvas maxrects-packer minimatch types semver electron@31.3.0 electron-package",
    "clear": "rm -rf deno.lock && rm -rf package-lock.json && rm -rf package.json && rm -rf node_modules"
  },
  "nodeModulesDir": "manual",
  "importMap": "../common/deps.json",
  "compilerOptions": {
    "lib": ["dom", "deno.ns"]
  }
}
