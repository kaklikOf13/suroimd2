{
  "tasks": {
    "dev": "deno run -A npm:vite",
    "build": "deno run -A npm:vite build",
    "install":"npm i vite && npm i @sveltejs/vite-plugin-svelte && npm i sass && npm i canvas && npm i maxrects-packer && deno install --allow-scripts=npm:canvas@3.1.2 && npm i minimatch",
    "clear":"rm -rf deno.lock && rm -rf package-lock.json && rm -rf package.json && rm -rf node_modules"
  },
  "nodeModulesDir": "auto",
  "importMap": "../common/config/deps.json",
  "compilerOptions": {
        "lib": ["dom", "deno.ns"]
    }
}
