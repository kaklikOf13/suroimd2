{
  "tasks": {
    "dev": "deno task dev:client & deno task dev:server",
    "dev:client": "cd client && deno task dev",
    "dev:server": "cd server && deno task dev:game",
    "clear":"rm -rf dist",
    "install":"cd client && deno task install"
  },
  "lock": false,
  "importMap": "./common/config/deps.json",
  "compilerOptions": {
    "lib": ["dom", "deno.ns"]
  }
}
